# Vertical Ecosystem Manager

Registry of 42 market verticals across 7 categories. Pure Ruby gem, namespace `VerticalEcosystemManager`.

Value objects: Vertical, Category, OntologyStandard, RayswarmMapping. Registry pattern for lookup, SeedHelper for DB population. Vertical data files in `verticals/`.

Dev: `bundle exec rake test` (Minitest).
